% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/one_way.R
\name{one_way}
\alias{one_way}
\alias{one_way.default}
\title{Create a one-way analysis plot}
\usage{
one_way(data, ...)

\method{one_way}{default}(
  data,
  var,
  obs = "target",
  exposure = "exposure",
  split = NA_character_,
  bins = 35L,
  type_agg = c("equal_exposure", "equal_range"),
  ret = c("plot", "data"),
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{data.table}.}

\item{...}{Arguments passed to methods.}

\item{var}{\verb{[character(1)]} Column to plot on the x-axis.}

\item{obs}{\verb{[character()]} One or more column names to summarise on
the y-axis (right axis). Default \code{"target"}.}

\item{exposure}{\verb{[character(1)]} Column of exposure weights. If the column
does not exist in \code{data}, every row is given weight 1.
Default \code{"exposure"}.}

\item{split}{\verb{[character(1) | NA]} Optional column to split lines by.
\code{NA} (default) produces a single set of lines.}

\item{bins}{\verb{[integer(1)]} Number of equal-exposure bins for numeric
variables with more than \code{bins} unique values. Default 35.}

\item{type_agg}{\verb{[character(1)]} Binning strategy for numeric variables:
\code{"equal_exposure"} (default) or \code{"equal_range"}.}

\item{ret}{\verb{[character(1)]} \code{"plot"} (default) returns a plotly
object; \code{"data"} returns the aggregated data.table.}
}
\value{
A plotly object, or a data.table when \code{ret = "data"}, or \code{NULL}
with a warning if the plot cannot be produced.
}
\description{
Bins a feature variable, computes exposure-weighted means of one or more
observed variables per bin, and returns a dual-axis plotly chart (bars =
exposure, lines = weighted means). Optionally splits by a second variable.
}
\examples{
\dontrun{
one_way(mtcars, var = "wt", obs = "mpg", bins = 10)
one_way(mtcars, var = "cyl", obs = c("mpg", "hp"), split = "am")
}

}
